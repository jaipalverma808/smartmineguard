[project]
name = "smartmineguard"
version = "1.0.0"
description = "Mining & Mineral Transport Monitoring System"
requires-python = ">=3.12"
dependencies = [
    "Flask==3.0.3",
    "Werkzeug==3.0.6",
    "python-dotenv==1.0.1",
    "gunicorn==22.0.0",
]

[tool.vercel]
entrypoint = "app:app"
